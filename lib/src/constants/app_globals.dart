class AppGlobals {
  static const appTitle = 'Catalog App';
  static const catalogTitle = 'Shopping Catalog';
  static const cartTitle = 'Shopping Cart';

  // Animations
  static const animDuration = Duration(milliseconds: 750);
  static const iconAnimDuration = Duration(milliseconds: 350);
}
